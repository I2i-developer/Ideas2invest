# Global rules for all crawlers
User-agent: *

# Allow main site content
Allow: /

# Block error & system pages
Disallow: /404
Disallow: /500
Disallow: /_next/          # Next.js build files
Disallow: /api/            # API routes not meant for indexing
Disallow: /static/         # Internal assets if not needed in search

# Prevent duplicate parameter URLs (tracking/query)
Disallow: /*?*utm_*
Disallow: /*?*fbclid=*

# Sitemap
Sitemap: https://www.ideas2invest.com/sitemap.xml